name: Build OpenSoundBoard

on:
  push:
    branches:
      - '**'
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            platform: win32
          - os: macos-latest
            platform: darwin
          - os: ubuntu-latest
            platform: linux

    runs-on: ${{ matrix.os }}
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Setup Python (for node-gyp)
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      # Linux: install system dependencies for uiohook-napi and RPM maker
      - name: Install Linux system dependencies
        if: matrix.platform == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libevdev-dev \
            libasound2-dev \
            libx11-dev \
            libxtst-dev \
            libpng-dev \
            libxkbcommon-x11-dev \
            rpm \
            dpkg \
            fakeroot

      # Windows: ensure Visual Studio build tools are available
      - name: Setup MSVC (Windows)
        if: matrix.platform == 'win32'
        uses: ilammy/msvc-dev-cmd@v1

      - name: Install dependencies
        run: npm ci
        env:
          # Prevent interactive prompts
          CI: true

      - name: Make distributables (Windows/Linux)
        if: matrix.platform != 'darwin'
        run: npm run make
        env:
          CI: true
          # Windows signing secrets
          WINDOWS_CERT_PATH: ${{ secrets.WINDOWS_CERT_PATH }}
          WINDOWS_CERT_PASSWORD: ${{ secrets.WINDOWS_CERT_PASSWORD }}

      - name: Make distributables (macOS)
        if: matrix.platform == 'darwin'
        run: npm run make -- --arch=universal
        env:
          CI: true
          # macOS signing and notarization secrets
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
          # Certificate comes from standard actions (setup-node doesn't do it, but assuming environment is prepped or user adds step)
          # For standard GitHub Actions, we'd need a step to import the cert. 
          # Assuming standard environment vars for `electron-forge` or `electron-builder` usage.

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: opensoundboard-${{ matrix.platform }}
          path: out/make/**/*
          retention-days: 30
